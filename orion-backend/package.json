{
  "name": "orion-backend",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "services/*",
    "shared",
    "scripts"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
