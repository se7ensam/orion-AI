# Docker Compose Override Example
# Copy this to docker-compose.override.yml to customize settings

version: '3.8'

services:
  download:
    environment:
      - MAX_WORKERS=10
    # Uncomment to add resource limits
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '2'
    #       memory: 4G
    #     reservations:
    #       cpus: '1'
    #       memory: 2G

  neo4j:
    # Uncomment to change Neo4j memory settings
    # environment:
    #   - NEO4J_dbms_memory_heap_max__size=4G
    #   - NEO4J_dbms_memory_heap_initial__size=2G

